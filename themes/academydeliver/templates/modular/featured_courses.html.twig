<h2>{{ page.header.title }}</h2>

  {% block content %}

    {{ page.content }}

    {% for post in taxonomy.findTaxonomy({'category':'courses','tag':'featured'}) %}
        <h3><a href="{{ post.url }}">{{ post.title }}</a></h3>
        <p>{{ post.summary }} <a class="button-secondary" href="{{ post.url }}">Read more</a></p>
    {% endfor %}

  {% endblock %}
